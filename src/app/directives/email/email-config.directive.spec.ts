import { EmailConfigDirective } from './email-config.directive';

describe('EmailConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailConfigDirective();
    expect(directive).toBeTruthy();
  });
});
